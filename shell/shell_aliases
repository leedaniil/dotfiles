# ---------------------------------------------------------
# Python
# ---------------------------------------------------------
alias mkvenv="mkvirtualenv $1"
alias rmvenv="rmvirtualenv $1"


# ---------------------------------------------------------
# SSH
# ---------------------------------------------------------
alias grace="ssh root@82.146.62.226" 


# ---------------------------------------------------------
# Docker
# ---------------------------------------------------------
alias docker-rm="docker rm $(docker ps -a | awk '{print $1}')"
alias docker-rmi="docker rmi $(docker images | awk '{print $3}')"